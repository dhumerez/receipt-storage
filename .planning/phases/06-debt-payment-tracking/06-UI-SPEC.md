---
phase: 6
slug: debt-payment-tracking
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-31
---

# Phase 6 — UI Design Contract: Debt & Payment Tracking

> Visual and interaction contract for Phase 6 frontend work. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No components.json; confirmed across Phases 3-5 |
| Preset | not applicable | Tailwind v4 via `@import 'tailwindcss'` in index.css |
| Component library | none (hand-rolled) | Established Phase 1; all components in frontend/src/components |
| Icon library | Inline SVG (Heroicons style) | Observed in Sidebar.tsx, TransactionDetailPage.tsx |
| Font | System UI (browser default) | No @font-face or Google Fonts import |

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), badge inline padding, status badge `px-2` |
| sm | 8px | Input inner padding, row gap between form fields (`gap-2`), payment history row internal gap |
| md | 16px | Default element spacing, card content padding (`p-4`), form field padding |
| lg | 24px | Section padding (`p-6`), debt detail header padding, payment form section margin (`mb-6`) |
| xl | 32px | Page padding (`p-8` = 32px — established pattern across all detail pages) |
| 2xl | 48px | Section separators between major debt detail sections |
| 3xl | 64px | Reserved; not directly used in Phase 6 |

Exceptions:
- Touch targets: minimum 44px height on all interactive elements — "Record Payment" button, "Write Off" button, payment history rows, "Approve Payment"/"Reject Payment" buttons. Apply via `min-h-[44px]`.
- WriteOffDialog modal: max-width 480px (`max-w-md`) — narrower than full-page forms since it contains only a textarea and two buttons.
- Badge vertical padding uses 2px (`py-0.5`) for compact pill appearance — intentional exception to the 4px-minimum scale, applied to `PaymentStatusBadge` and portal annotation badges.

---

## Typography

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| Body | 14px (`text-sm`) | 400 regular | 1.5 | Established across all phases — table cells, labels, payment row text |
| Label | 14px (`text-sm`) | 400 regular | 1.4 | Form labels: `text-sm font-normal text-gray-700` |
| Heading | 24px (`text-2xl`) | 600 semibold | 1.2 | Page titles: `text-2xl font-semibold text-gray-900` — debt detail header |
| Sub-heading | 20px (`text-xl`) | 600 semibold | 1.2 | Dialog titles: `text-xl font-semibold text-gray-900` — WriteOffDialog |

Only 2 weights: regular (400) and semibold (600). No bold, no medium.

Phase 6 specifics:
- Section labels within DebtDetailPage (e.g., "Payment History", "Record Payment", "Original Transaction"): `text-sm font-semibold text-gray-700` (14px, 600 weight — matches TransactionDetailPage section pattern).
- Payment status badge: `text-xs font-semibold` (12px, 600 weight — matches TransactionStatusBadge).
- "Does not affect balance" annotation on pending payments: `text-xs font-normal text-gray-500` (12px, 400 weight).
- Write-off reason inline display: `text-sm font-normal text-gray-600 italic` (14px, 400 weight).
- Money amounts in debt header (Original, Paid, Remaining): `text-2xl font-semibold text-gray-900` for the primary remaining balance; `text-sm font-normal text-gray-700` for original and paid sub-values.

---

## Color

| Role | Value | Usage | Source |
|------|-------|-------|--------|
| Dominant (60%) | `gray-50` (#F9FAFB) | Page background (`bg-gray-50`) | AppLayout.tsx established pattern |
| Secondary (30%) | `white` (#FFFFFF) | Debt detail card surfaces, payment form container, dialog surfaces | Cards: `bg-white border border-gray-200 rounded-lg` |
| Accent (10%) | `blue-600` (#2563EB) | See reserved list below | Established Phases 3-5 |
| Destructive | `red-600` / `red-700` | Write-off confirm button, reject payment button, error banners | Phase 5 pattern |

Accent (`blue-600`) reserved for:
1. Primary CTA buttons — "Record Payment" (submit), "Reopen Debt"
2. Back link text — "Back to Client" link in debt detail header
3. Client name link — clickable link to `/clients/:id` in debt header
4. Transaction link — clickable link to `/transactions/:id` in original transaction section
5. Focus rings — `focus:ring-2 focus:ring-blue-500` on all inputs in payment form
6. "Record Payment" expand button — `bg-blue-600 text-white hover:bg-blue-700` primary button style

NOT accent: payment history row hover (use `hover:bg-gray-50`), status badges (use semantic colors below), write-off button (use destructive red or gray secondary).

### Debt Status Badge Colors

| Status | Background | Text | Source |
|--------|-----------|------|--------|
| `open` | `bg-green-100` | `text-green-800` | DebtCard.tsx existing |
| `partially_paid` | `bg-blue-100` | `text-blue-700` | DebtCard.tsx existing |
| `fully_paid` | `bg-green-100` | `text-green-800` | DebtCard.tsx existing |
| `written_off` | `bg-gray-100` | `text-gray-600` | DebtCard.tsx existing |

### Payment Status Badge Colors

| Status | Background | Text |
|--------|-----------|------|
| `confirmed` | `bg-green-100` | `text-green-800` |
| `pending_approval` | `bg-yellow-100` | `text-yellow-800` |
| `rejected` | `bg-red-100` | `text-red-700` |

---

## Copywriting Contract

### Primary CTAs

| Element | Copy | Context |
|---------|------|---------|
| Expand payment form | "Record Payment" | Button that expands inline form below payment history (D-01) |
| Submit payment | "Save Payment" | Submit button inside expanded payment form |
| Cancel payment form | "Cancel" | Collapses payment form without saving |
| Write-off trigger | "Write Off" | Button in debt header, owner-only (D-08) |
| Write-off confirm | "Write Off Debt" | Confirm button in WriteOffDialog (D-09) |
| Write-off cancel | "Cancel" | Dismiss dialog without action |
| Reopen debt | "Reopen Debt" | Button in debt header when status is `written_off`, owner-only (D-10) |
| Approve payment | "Approve Payment" | Inline action on pending payment row |
| Reject payment | "Reject Payment" | Triggers inline reason input on pending payment row |
| Reject confirm | "Confirm Reject" | After typing rejection reason |
| Back link | "Back to Client" | Navigates to `/clients/:clientId` from debt detail |

### Empty States

| Page | Heading | Body | CTA |
|------|---------|------|-----|
| Debt detail — no payments yet | "No payments recorded" | "Record the first payment to start tracking this debt." | "Record Payment" (expands form) |
| Portal debt detail — no payments | "No payments yet" | "Payments will appear here once they are recorded." | none |

### Error States

| Context | Copy |
|---------|------|
| Debt detail load failure | "Could not load this debt. Please refresh the page." |
| Payment save failure | "Couldn't save payment. Check your connection and try again." |
| Payment exceeds remaining balance | "Payment amount exceeds the remaining balance of $X.XX." |
| Write-off failure | "Couldn't write off this debt. Please try again." |
| Reopen failure | "Couldn't reopen this debt. Please try again." |
| Approval action failure | "Couldn't process that action. Please try again." |
| Reject without reason | "A reason is required to reject a payment." |
| File type rejected (client-side) | "That file type isn't supported. Upload JPEG, PNG, WebP, PDF, or HEIC files." |
| File too large (client-side) | "That file is too large. Maximum file size is 10 MB." |

### Destructive Actions

| Action | Trigger | Confirmation Approach |
|--------|---------|----------------------|
| Write off debt | "Write Off" button in header (owner-only) | Modal dialog (WriteOffDialog): textarea for reason (`placeholder="Describe why this debt is being written off"`, `maxLength={500}`, `required`), "Write Off Debt" button `bg-red-600 text-white hover:bg-red-700`, "Cancel" text button. (D-09) |
| Reject pending payment | "Reject Payment" button on payment row | Inline expansion: textarea appears below the payment row labeled "Reason for rejection", "Confirm Reject" button `bg-red-600 text-white hover:bg-red-700`, "Cancel" link to collapse. Matches Phase 5 notification panel reject pattern. |

---

## Component Inventory

New components required for Phase 6:

| Component | Path | Description |
|-----------|------|-------------|
| `PaymentStatusBadge` | `components/debts/PaymentStatusBadge.tsx` | Badge for `confirmed`, `pending_approval`, `rejected` states. Pill style matching TransactionStatusBadge. |
| `PaymentForm` | `components/debts/PaymentForm.tsx` | Inline expandable form: Amount, Payment Date, Payment Method dropdown, Reference, Notes, FileAttachmentSection. Submitted as multipart FormData. |
| `PaymentHistoryList` | `components/debts/PaymentHistoryList.tsx` | Chronological list (newest first). Each row: amount, paid_at, method + reference, status badge, proof doc thumbnails. Pending rows annotated "Does not affect balance". |
| `WriteOffDialog` | `components/debts/WriteOffDialog.tsx` | Modal dialog with required reason textarea and "Write Off Debt" confirm button. Backdrop pattern from existing modals. |
| `DebtDetailPage` | `pages/debts/DebtDetailPage.tsx` | Full debt detail at `/debts/:id`. Sections: Header, Payment History, Record Payment (inline expand), Original Transaction link. |
| `PortalDebtDetailPage` | `pages/portal/PortalDebtDetailPage.tsx` | Client portal view at `/portal/debts/:id`. Same layout minus Record Payment form, Write Off button, internal notes (D-14). |

Reused components (no changes or minor changes):

| Component | Change Needed |
|-----------|---------------|
| `DebtCard` | Add `<Link to={/debts/${debt.id}}>` wrapping the card (D-05) |
| `PortalDebtGroup` / `PortalTransactionRow` | Add link navigation to `/portal/debts/:id` |
| `FileAttachmentSection` | No changes — drop-in reuse for payment proof upload (D-03) |
| `EmptyState` | No changes — reuse for "No payments recorded" state |
| `AppLayout` | No changes — existing layout accommodates new routes |

---

## Interaction Contracts

### Debt Detail Page Layout (D-06)

- Page padding: `p-8` (32px) matching TransactionDetailPage.
- Back link: "Back to Client" as `text-sm text-blue-600 hover:text-blue-700` at top of page.
- Section separator: `border-t border-gray-200 mt-8 pt-8` between sections — same as TransactionDetailPage.
- Four sections stacked vertically:

**Section 1 — Header:**
- Client name as clickable link (`text-sm text-blue-600 hover:text-blue-700`) to `/clients/:clientId`.
- Debt status badge inline after a visual spacer.
- Three money values in a `grid grid-cols-3 gap-4` row: Original Amount, Total Paid, Remaining Balance. Each has a `text-gray-500 text-xs mb-0.5` label above the value — mirrors DebtCard grid pattern.
- Remaining balance: `text-2xl font-semibold`. Green (`text-green-700`) when $0.00; default gray-900 otherwise.
- If `written_off`: show reason in `text-sm text-gray-600 italic` below the status badge (D-11).
- Owner-only actions in header: "Write Off" button (when status is `open` or `partially_paid`) or "Reopen Debt" button (when status is `written_off`).
  - "Write Off": `border border-gray-300 text-gray-700 hover:bg-gray-50 px-3 py-2 rounded-md text-sm` — secondary style, not red (the dialog confirm is red).
  - "Reopen Debt": `bg-blue-600 text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm` — accent primary style.

**Section 2 — Payment History:**
- Section label: `text-sm font-semibold text-gray-700 mb-3`.
- Single chronological list, newest first (D-07).
- Each payment row: `bg-white border border-gray-200 rounded-lg p-4 mb-2` card-style.
  - Row layout: `flex items-start justify-between` with left content and right amount.
  - Left: payment date (`text-sm text-gray-900`), method + reference as "transfer - Ref TRF-88821" (`text-sm text-gray-500`), `PaymentStatusBadge`, recorded-by name (`text-xs text-gray-400`).
  - Right: amount `text-sm font-semibold text-gray-900`.
  - If `pending_approval`: show `text-xs text-gray-500` annotation "Does not affect balance" below the badge.
  - If payment has proof documents: thumbnail row `flex gap-2 mt-2` using same AttachmentThumbnail pattern from TransactionDetailPage (link to authenticated download, `w-16 h-16 rounded-md object-cover`).
  - Owner sees "Approve Payment" / "Reject Payment" buttons on `pending_approval` rows: `flex gap-2 mt-2`.
    - "Approve Payment": `bg-blue-600 text-white hover:bg-blue-700 px-3 py-1.5 rounded-md text-xs font-semibold`.
    - "Reject Payment": `border border-gray-300 text-gray-700 hover:bg-gray-50 px-3 py-1.5 rounded-md text-xs font-semibold`.
- If no payments: render `EmptyState` with heading "No payments recorded", body "Record the first payment to start tracking this debt.", CTA "Record Payment".

**Section 3 — Record Payment (D-01):**
- Only visible to `owner` and `collaborator` roles. Hidden for `viewer` and on portal.
- Only visible when debt status is `open` or `partially_paid` (not `fully_paid` or `written_off`).
- Default state: single button "Record Payment" — `bg-blue-600 text-white hover:bg-blue-700 px-4 py-2 rounded-md text-sm font-semibold`.
- Expanded state: `PaymentForm` component appears below the button (button is replaced by the form).
- Form container: `bg-white border border-gray-200 rounded-lg p-6`.
- Form fields stacked with `space-y-4`:
  - Amount: `<input type="number" min="0.01" step="0.01">` with `max` set to remaining balance. Label "Amount *".
  - Payment Date: `<input type="date">`. Label "Payment Date *". Default to today.
  - Payment Method: `<select>` with options: Cash, Transfer, Mobile Payment, Other. When "Other" selected, a text input appears below. Label "Payment Method *".
  - Reference Number: `<input type="text">`. Label "Reference Number". Optional.
  - Notes: `<textarea rows={2} resize-none>`. Label "Notes". Optional.
  - Proof Documents: `FileAttachmentSection` — full reuse, same Take Photo + Choose from Gallery UX (D-03).
- Form actions: `flex justify-end gap-3 mt-4`. "Cancel" as `text-sm text-gray-600 hover:text-gray-900`, "Save Payment" as `bg-blue-600 text-white hover:bg-blue-700 px-4 py-2 rounded-md text-sm font-semibold`.
- All inputs: `w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm` — established pattern.
- On success: collapse form, show success toast (discretionary), invalidate debt query cache.

**Section 4 — Original Transaction:**
- Link to `/transactions/:transactionId`: "View Transaction {referenceNumber}" as `text-sm text-blue-600 hover:text-blue-700`.
- If transaction has attached documents: thumbnail row below the link using AttachmentThumbnail pattern.

### Reject Payment Inline Expansion

- Click "Reject Payment" on a pending payment row: row expands to show a `<textarea rows={2}>` labeled "Reason for rejection" and "Confirm Reject" button below.
- "Reject Payment" button hidden during expansion (replaced by "Confirm Reject" and "Cancel" link).
- Textarea: `placeholder="Describe why this payment is being rejected"`, `maxLength={500}`, `required`.
- "Confirm Reject": `bg-red-600 text-white hover:bg-red-700 px-3 py-1.5 rounded-md text-xs font-semibold`.
- "Cancel": `text-xs text-gray-500 hover:text-gray-700 ml-2`.
- If reason is empty on submit: show inline validation "A reason is required to reject a payment." in `text-xs text-red-600`.

### WriteOffDialog

- Trigger: "Write Off" button in debt header.
- Modal backdrop: `fixed inset-0 z-50 bg-black/50 flex items-center justify-center` — matches existing modal pattern (ProductModal, ClientModal).
- Dialog container: `bg-white rounded-lg shadow-xl max-w-md w-full mx-4 p-6`.
- Title: "Write Off Debt" at `text-xl font-semibold text-gray-900`.
- Description: "This will mark the debt as uncollectible. You can reopen it later if needed." at `text-sm text-gray-500 mt-1 mb-4`.
- Textarea: `rows={3}`, `placeholder="Describe why this debt is being written off"`, `maxLength={500}`, `required`. Same input styling as all other textareas.
- Actions: `flex justify-end gap-3 mt-4`. "Cancel" as `text-sm text-gray-600 hover:text-gray-900`, "Write Off Debt" as `bg-red-600 text-white hover:bg-red-700 px-4 py-2 rounded-md text-sm font-semibold`.
- Close on backdrop click or Cancel. Do not close on Escape if textarea has content (prevent accidental loss).

### Portal Debt Detail (D-12, D-13, D-14)

- Same structural layout as owner DebtDetailPage.
- Back link: "Back to Dashboard" navigates to `/portal`.
- Header: identical money grid and status badge. No "Write Off" or "Reopen" buttons.
- Payment History: identical list. Pending payments show "Awaiting confirmation" annotation instead of "Does not affect balance" — same visual style (`text-xs text-yellow-700 bg-yellow-50 px-2 py-0.5 rounded`). No "Approve Payment"/"Reject Payment" buttons.
- No Record Payment section.
- Original Transaction section: show transaction reference number as plain text (not a link — portal users do not have access to `/transactions/:id`).
- Proof documents on payments: visible and downloadable (same AttachmentThumbnail pattern with authenticated URLs).

### Loading States

- Debt detail page loading: `text-sm text-gray-400` with text "Loading debt details..." inside `p-8` container — matches TransactionDetailPage pattern.
- Payment form submit: disable "Save Payment" button and show "Saving..." text. Re-enable on error.
- Write-off/Reopen actions: disable button during mutation. Show "Processing..." text.
- Approve/Reject: disable both buttons on the row during mutation.

### Payment Method Dropdown (D-02)

- Standard `<select>` element with same input styling.
- Options: `["Cash", "Transfer", "Mobile Payment", "Other"]`.
- When "Other" is selected: a text input appears directly below with `placeholder="Enter payment method"`, `required`. Animated with simple show/hide (no transition needed).
- Selected method value sent to API: the preset string for preset options, the custom text for "Other".

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required — no shadcn installed |
| Third-party | none | not applicable |

No component library or third-party registry is used. All components are hand-rolled with Tailwind v4 utility classes, consistent with Phases 1-5.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
