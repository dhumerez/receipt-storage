---
phase: 05-transactions-file-uploads
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/components/layout/NotificationBell.tsx
  - frontend/src/components/layout/NotificationPanel.tsx
  - frontend/src/components/layout/NotificationPanelItem.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/BottomTabBar.tsx
  - frontend/src/__tests__/ApprovalWorkflow.test.tsx
autonomous: true
requirements:
  - FR-09.1
  - FR-09.2
  - FR-09.3
  - FR-09.4

must_haves:
  truths:
    - "Bell icon with unread count badge visible on every page for owner and collaborator"
    - "Clicking bell opens a right-side slide-out panel with pending transactions"
    - "Owner can approve or reject transactions inline from the panel"
    - "Reject requires a reason with inline expansion"
    - "Collaborator sees their own submissions and statuses in the panel"
    - "Badge count updates after approve/reject actions"
  artifacts:
    - path: "frontend/src/components/layout/NotificationBell.tsx"
      provides: "Bell icon button with unread count badge"
      min_lines: 30
    - path: "frontend/src/components/layout/NotificationPanel.tsx"
      provides: "Right-side slide-out notification panel"
      min_lines: 60
    - path: "frontend/src/components/layout/NotificationPanelItem.tsx"
      provides: "Individual notification item with approve/reject actions"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/layout/NotificationBell.tsx"
      to: "/api/v1/notifications/unread-count"
      via: "useQuery with getUnreadCount"
      pattern: "getUnreadCount"
    - from: "frontend/src/components/layout/NotificationPanel.tsx"
      to: "/api/v1/notifications"
      via: "useQuery with getNotifications"
      pattern: "getNotifications"
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "frontend/src/components/layout/NotificationBell.tsx"
      via: "header element in AppLayout"
      pattern: "NotificationBell"
---

<objective>
Build the notification bell icon with unread badge, the right-side slide-out panel, and inline approve/reject actions. Integrate the bell into AppLayout header. Add Transactions link to Sidebar and BottomTabBar navigation.

Purpose: Owners need to see and act on pending approvals from any page (FR-09.1, FR-09.2, FR-09.4). Collaborators need to see their submission statuses (D-16).
Output: NotificationBell + NotificationPanel + NotificationPanelItem components, updated AppLayout with bell in header, Sidebar and BottomTabBar with Transactions nav link, Wave 0 test stubs for approval workflow.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-transactions-file-uploads/05-CONTEXT.md
@.planning/phases/05-transactions-file-uploads/05-UI-SPEC.md
@.planning/phases/05-transactions-file-uploads/05-01-SUMMARY.md

<interfaces>
<!-- API functions from frontend/src/api/transactions.ts (Plan 03 Task 1) -->
export function getNotifications(): Promise<NotificationItem[]>
export function getUnreadCount(): Promise<{ count: number }>
export function markNotificationRead(id: string): Promise<void>
export function markAllNotificationsRead(): Promise<void>
export function approveTransaction(id: string): Promise<TransactionDetail>
export function rejectTransaction(id: string, reason: string): Promise<void>

export interface NotificationItem {
  id: string; entityType: string; entityId: string; action: string;
  isRead: boolean; createdAt: string;
  referenceNumber: string; totalAmount: string; clientName: string; submitterName: string;
}

<!-- AppLayout current structure -->
<div className="min-h-screen bg-gray-50 flex">
  <Sidebar />
  <main className="flex-1 overflow-auto pb-16 md:pb-0"><Outlet /></main>
  <BottomTabBar />
</div>

<!-- Auth context -->
import { useAuth } from '../../contexts/AuthContext.tsx';
// user.role: 'owner' | 'collaborator' | 'viewer' | 'client'

<!-- Sidebar nav pattern (inline SVG icons) -->
// NavLink with: border-l-4 border-blue-600 bg-blue-50 text-blue-600 for active
// min-h-[44px] touch targets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationBell + NotificationPanel + NotificationPanelItem</name>
  <files>frontend/src/components/layout/NotificationBell.tsx, frontend/src/components/layout/NotificationPanel.tsx, frontend/src/components/layout/NotificationPanelItem.tsx</files>
  <read_first>
    - frontend/src/api/transactions.ts (notification API functions — created in Plan 03 Task 1)
    - frontend/src/components/products/ProductModal.tsx (modal overlay pattern: fixed inset-0)
    - frontend/src/contexts/AuthContext.tsx (useAuth, user.role)
    - .planning/phases/05-transactions-file-uploads/05-UI-SPEC.md (Bell Icon + Notification Panel interaction contract, Reject Inline Expansion)
    - .planning/phases/05-transactions-file-uploads/05-CONTEXT.md (D-12 through D-16)
  </read_first>
  <action>
**Create `frontend/src/components/layout/NotificationBell.tsx`:**

Renders for `owner` and `collaborator` roles only — return null for `viewer` and `client` (D-12).

Uses `useQuery({ queryKey: ['notifications', 'unread-count'], queryFn: getUnreadCount, staleTime: 30_000, refetchInterval: 60_000 })`.

Bell icon: inline SVG (Heroicons outline bell, 24x24). Wrapped in `<button>` with `relative p-2 text-gray-500 hover:text-gray-700 rounded-md`, `aria-label="Notifications"`.

Unread badge: `absolute -top-1 -right-1` circular `bg-blue-600 text-white` element, `min-w-[18px] h-[18px] rounded-full text-xs flex items-center justify-center`. Show count if <= 9, show "9+" if > 9. Hidden when count === 0.

State: `isPanelOpen: boolean`. Click toggles panel. Render `<NotificationPanel isOpen={isPanelOpen} onClose={() => setIsPanelOpen(false)} />` when open.

**Create `frontend/src/components/layout/NotificationPanel.tsx`:**

Props: `isOpen: boolean`, `onClose: () => void`.

Overlay: `fixed inset-0 bg-transparent z-50` to capture outside clicks (click closes panel).

Panel: `fixed inset-y-0 right-0 w-96 bg-white shadow-xl z-50` on desktop; `w-full` on mobile (< 640px: `sm:w-96 w-full`).

Animation: `transform transition-transform duration-200 ease-in-out`. Open: `translate-x-0`. Closed: `translate-x-full`. Render with conditional transform class.

Header: "Notifications" heading `text-lg font-semibold text-gray-900` + close button [x] `aria-label="Close notifications"` + "Mark all read" text link `text-sm text-blue-600 hover:text-blue-700`.

Content: `useQuery({ queryKey: ['notifications'], queryFn: getNotifications })`.

For `owner` role: filter to show items with action `'submitted_for_approval'` that are relevant. Each item renders as `<NotificationPanelItem>`.

For `collaborator` role (D-16): show their own submissions with current status (approved/rejected/pending). No approve/reject buttons.

Empty state: owner sees "You're all caught up" / "No transactions waiting for your approval." Collaborator sees "No submissions yet" / "Your submitted transactions will appear here."

Scrollable content area: `overflow-y-auto flex-1`.

**Create `frontend/src/components/layout/NotificationPanelItem.tsx`:**

Props: `notification: NotificationItem`, `userRole: string`.

Renders: reference number (`text-xs font-normal text-gray-500`), client name, total amount formatted as `$X.XX`, submitter name. Touch target: `min-h-[44px] p-3 border-b border-gray-100`.

For `owner` role, show two action buttons:
- "Approve": `bg-blue-600 text-white hover:bg-blue-700 px-3 py-1 rounded text-sm`. Calls `useMutation(approveTransaction)`. On success: `queryClient.invalidateQueries({ queryKey: ['notifications'] })` (Pitfall 8 fix).
- "Reject": `border border-gray-300 text-gray-700 hover:bg-gray-50 px-3 py-1 rounded text-sm`.

Reject inline expansion (D-15, UI-SPEC Reject Inline Expansion):
- Clicking "Reject" hides the Reject button and shows: `<textarea rows={2} placeholder="Describe why this transaction is being rejected" maxLength={500}>` + "Confirm Reject" button `bg-red-600 text-white hover:bg-red-700 px-3 py-1 rounded text-sm` + "Cancel" text link to collapse.
- Textarea required validation: if empty, show "A reason is required to reject a transaction." inline in `text-xs text-red-600`.
- "Confirm Reject" calls `useMutation(rejectTransaction)`. On success: invalidate queries.

For `collaborator` role (D-16): no action buttons. Show status badge inline using TransactionStatusBadge pattern:
- `submitted_for_approval` action: show "Pending" badge.
- `approved` action: show "Approved" badge.
- `rejected` action: show "Rejected" badge with rejection reason if available.

Error state on action failure: show "Couldn't process that action. Please try again." in `text-xs text-red-600` below the item.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/layout/NotificationBell.tsx contains `aria-label="Notifications"`
    - NotificationBell.tsx contains `bg-blue-600 text-white` (badge color)
    - NotificationBell.tsx contains `getUnreadCount`
    - NotificationBell.tsx contains `staleTime: 30_000` or `staleTime: 30000`
    - NotificationBell.tsx contains role check for `owner` or `collaborator`
    - frontend/src/components/layout/NotificationPanel.tsx contains `translate-x-full` (animation)
    - NotificationPanel.tsx contains `translate-x-0` (animation)
    - NotificationPanel.tsx contains `duration-200`
    - NotificationPanel.tsx contains `w-96` (panel width)
    - NotificationPanel.tsx contains `aria-label="Close notifications"`
    - NotificationPanel.tsx contains `You're all caught up` (empty state)
    - frontend/src/components/layout/NotificationPanelItem.tsx contains `approveTransaction`
    - NotificationPanelItem.tsx contains `rejectTransaction`
    - NotificationPanelItem.tsx contains `Confirm Reject`
    - NotificationPanelItem.tsx contains `bg-red-600` (reject confirm button)
    - NotificationPanelItem.tsx contains `invalidateQueries` (Pitfall 8 fix)
    - NotificationPanelItem.tsx contains `A reason is required`
  </acceptance_criteria>
  <done>Bell icon shows unread badge, slide-out panel lists pending transactions, owner can approve/reject inline with reason expansion, collaborator sees submission statuses, badge updates after actions.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate bell into AppLayout + add Transactions nav link + test stubs</name>
  <files>frontend/src/components/layout/AppLayout.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/BottomTabBar.tsx, frontend/src/__tests__/ApprovalWorkflow.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/AppLayout.tsx (current structure)
    - frontend/src/components/layout/Sidebar.tsx (nav link pattern, SVG icons)
    - frontend/src/components/layout/BottomTabBar.tsx (mobile nav pattern)
    - frontend/src/components/layout/NotificationBell.tsx (bell component created in Task 1)
  </read_first>
  <action>
**Update `frontend/src/components/layout/AppLayout.tsx`:**

Add a header bar between `<Sidebar />` and `<main>`. The main content area now includes a top bar with the bell icon.

New structure:
```tsx
<div className="min-h-screen bg-gray-50 flex">
  <Sidebar />
  <div className="flex-1 flex flex-col overflow-hidden">
    {/* Top header bar */}
    <header className="bg-white border-b border-gray-200 px-4 py-2 flex items-center justify-end">
      <NotificationBell />
    </header>
    {/* Main content */}
    <main className="flex-1 overflow-auto pb-16 md:pb-0">
      <Outlet />
    </main>
  </div>
  <BottomTabBar />
</div>
```

Import `NotificationBell` from `./NotificationBell.tsx`.

The bell is positioned at the right end of the header bar (`justify-end`), per Claude's discretion on bell position.

**Update `frontend/src/components/layout/Sidebar.tsx`:**

Add a "Transactions" nav link between "Clients" and "Products" (or after Products — follow existing alphabetical or logical order). Use the same `NavLink` pattern with inline SVG icon (use a receipt/document icon from Heroicons outline style — a rectangle with lines).

The link:
- Path: `/transactions`
- Label: "Transactions"
- Icon: inline SVG, same 20x20 size as other nav icons
- Active state: `border-l-4 border-blue-600 bg-blue-50 text-blue-600` (same as existing)
- Touch target: `min-h-[44px]` (same as existing)

**Update `frontend/src/components/layout/BottomTabBar.tsx`:**

Add "Transactions" tab between existing tabs. Same pattern as other tabs:
- Icon: same inline SVG as sidebar (scaled to fit mobile tab)
- Label: "Transactions" in `text-xs`
- NavLink to `/transactions`
- Active styling consistent with existing tabs

**Create `frontend/src/__tests__/ApprovalWorkflow.test.tsx`** — Wave 0 test stubs:

```typescript
import { describe, it } from 'vitest';

describe('NotificationBell', () => {
  it.todo('renders for owner role');
  it.todo('renders for collaborator role');
  it.todo('returns null for viewer role');
  it.todo('returns null for client role');
  it.todo('shows badge with unread count');
  it.todo('hides badge when count is 0');
  it.todo('shows 9+ when count exceeds 9');
  it.todo('toggles panel on click');
});

describe('NotificationPanel', () => {
  it.todo('slides in from right when open');
  it.todo('shows pending transactions for owner');
  it.todo('shows submission statuses for collaborator');
  it.todo('shows empty state when no notifications');
  it.todo('marks all as read on link click');
});

describe('NotificationPanelItem', () => {
  it.todo('shows approve and reject buttons for owner');
  it.todo('hides action buttons for collaborator');
  it.todo('expands reject reason textarea on reject click');
  it.todo('requires reason before confirm reject');
  it.todo('invalidates queries after approve');
  it.todo('invalidates queries after reject');
});
```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/layout/AppLayout.tsx contains `NotificationBell`
    - AppLayout.tsx contains `<header` element
    - AppLayout.tsx contains `border-b border-gray-200` (header border)
    - frontend/src/components/layout/Sidebar.tsx contains `/transactions` link
    - Sidebar.tsx contains `Transactions` text
    - frontend/src/components/layout/BottomTabBar.tsx contains `/transactions` link
    - BottomTabBar.tsx contains `Transactions` text
    - frontend/src/__tests__/ApprovalWorkflow.test.tsx exists with test stubs
  </acceptance_criteria>
  <done>Bell icon visible in AppLayout header on every page. Sidebar and BottomTabBar include Transactions navigation link. Approval workflow test stubs created for Wave 0 compliance.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` compiles without errors
- NotificationBell renders for owner/collaborator, hidden for viewer/client
- Panel slides in from right with animation
- Approve/reject mutations invalidate notification queries
- Sidebar and BottomTabBar have Transactions link
- ApprovalWorkflow test stubs exist
</verification>

<success_criteria>
Bell icon with unread count badge is visible in AppLayout header. Clicking opens slide-out panel showing pending approvals (owner) or submission statuses (collaborator). Owner can approve/reject inline. Reject requires reason with inline expansion. Badge count updates after actions. Transactions navigation link added to sidebar and mobile tab bar.
</success_criteria>

<output>
After completion, create `.planning/phases/05-transactions-file-uploads/05-04-SUMMARY.md`
</output>
