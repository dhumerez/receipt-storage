---
phase: 01-foundation-infrastructure-database
plan: 03
subsystem: database
tags: [drizzle-orm, postgresql, schema, migrations, typescript]

# Dependency graph
requires:
  - phase: 01-01
    provides: Docker Compose stack, backend Dockerfile, directory structure

provides:
  - Complete Drizzle ORM schema for all 12 tables
  - debt_balances view with confirmed-only payment filtering
  - forCompany() tenant-scoped query helper
  - Initial SQL migration (0000_init.sql) generated by drizzle-kit
  - Audit log immutability SQL (0001_audit_log_immutability.sql) with executable REVOKE

affects:
  - Phase 2 (Auth & Users) — users table schema, password_hash column, roles enum
  - Phase 3 (Client Management) — clients table, forCompany().clients helper
  - Phase 4 (Product Catalog) — products table, forCompany().products helper
  - Phase 5 (Transactions & Files) — transactions, transaction_items, documents tables
  - Phase 6 (Debt & Payments) — debts, payments tables, debt_balances view
  - Phase 7 (Reports) — debt_balances view, company_counters for reference numbers

# Tech tracking
tech-stack:
  added:
    - drizzle-orm 0.45.2
    - drizzle-kit 0.31.10
    - pg 8.20.0
    - dotenv 17.3.1
    - bcryptjs 3.0.3
    - express 5.2.1
    - helmet 8.1.0
    - jsonwebtoken 9.0.3
    - zod 4.3.6
    - tsx 4.21.0
    - typescript 6.0.2
    - vitest ^3.0.0
  patterns:
    - pgEnum for all status columns (not VARCHAR + CHECK constraint)
    - NUMERIC(12,2) for all money columns (never FLOAT or INTEGER cents)
    - All tenant-scoped tables have company_id FK to companies
    - generated column for transaction_items.line_total (quantity * unit_price)
    - pgView for debt_balances to avoid storing derived balance
    - forCompany() helper enforces tenant isolation at query level
    - Append-only audit_logs with DB-level REVOKE for immutability

key-files:
  created:
    - backend/package.json
    - backend/tsconfig.json
    - backend/drizzle.config.ts
    - backend/.gitignore
    - backend/src/db/schema.ts
    - backend/src/db/client.ts
    - backend/src/db/migrate.ts
    - backend/src/db/query-helpers.ts
    - backend/src/db/migrations/0000_init.sql
    - backend/src/db/migrations/0001_audit_log_immutability.sql
  modified: []

key-decisions:
  - "drizzle-orm pinned to 0.45.2 (not 0.46.x or 1.x beta) — breaking changes in newer versions"
  - "All money columns use NUMERIC(12,2) — no FLOAT, no INTEGER cents — immutable decision after initial migration"
  - "debt_balances view computes remaining_balance from confirmed payments only — prevents pending payments from reducing displayed balance"
  - "audit_logs immutability enforced at DB level with REVOKE UPDATE, DELETE — application layer alone is insufficient (FR-11.2)"
  - "company_counters table with unique(company_id, year) enables per-company per-year reference number sequencing (NFR-04.3)"
  - "File storage on disk — no S3; all file access via authenticated Express endpoint (not Nginx static)"

patterns-established:
  - "Pattern 1: forCompany(companyId) — all queries start here, companyId from JWT only"
  - "Pattern 2: pgEnum for status columns — generates proper Postgres enum types, not CHECK constraints"
  - "Pattern 3: Never store computed balances — always compute from debt_balances view"
  - "Pattern 4: Financial records are never hard-deleted — use voided_at/void_reason/status fields"

requirements-completed:
  - FR-01.1
  - FR-01.3
  - FR-01.4
  - FR-02.1
  - FR-05.2
  - FR-07.3
  - FR-07.4
  - FR-11.1
  - FR-11.2
  - NFR-01.2
  - NFR-02.1
  - NFR-02.2
  - NFR-02.3
  - NFR-04.3

# Metrics
duration: 5min
completed: 2026-03-30
---

# Phase 01 Plan 03: Drizzle ORM Schema Summary

**Complete 12-table Drizzle schema with debt_balances view, tenant query helper, and audit log immutability enforcement via REVOKE UPDATE/DELETE.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-30T00:22:34Z
- **Completed:** 2026-03-30T00:27:24Z
- **Tasks:** 3 (Task 1, Task 2a, Task 2b)
- **Files modified:** 10

## Accomplishments

- Initialized backend project with exact pinned versions (drizzle-orm 0.45.2, drizzle-kit 0.31.10, express 5.2.1)
- Defined all 12 tables with proper types: NUMERIC(12,2) for money, pgEnum for all status columns, UUID primary keys
- Generated initial SQL migration via drizzle-kit confirming 12 tables, all columns and FK relationships correct
- Created forCompany() tenant isolation helper covering all 7 scoped entity types
- Established audit log immutability with executable REVOKE UPDATE, DELETE SQL (FR-11.2)

## Task Commits

Each task was committed atomically:

1. **Task 1: Initialize backend package.json, tsconfig.json, drizzle.config.ts** - `dc7db1f` (chore)
2. **Task 2a: Write Drizzle schema — companies, users, clients, products, enums** - `13fb285` (feat)
3. **Task 2b: Complete schema, query-helpers, migration files** - `ab9c16c` (feat)

**Plan metadata:** (docs commit — see final_commit step)

## Files Created/Modified

- `backend/package.json` - Project dependencies with pinned drizzle-orm 0.45.2
- `backend/tsconfig.json` - TypeScript config targeting ES2022, NodeNext modules
- `backend/drizzle.config.ts` - Drizzle Kit config pointing to src/db/migrations
- `backend/.gitignore` - Excludes node_modules, dist, .env
- `backend/src/db/schema.ts` - All 12 tables + debt_balances view + 5 pgEnums
- `backend/src/db/client.ts` - Drizzle DB singleton using pg Pool, max 10 connections
- `backend/src/db/migrate.ts` - Programmatic migration runner for docker-entrypoint
- `backend/src/db/query-helpers.ts` - forCompany() tenant-scoped query builder
- `backend/src/db/migrations/0000_init.sql` - Initial migration generated by drizzle-kit
- `backend/src/db/migrations/0001_audit_log_immutability.sql` - Executable REVOKE statements

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- backend/package.json: FOUND
- backend/tsconfig.json: FOUND
- backend/drizzle.config.ts: FOUND
- backend/src/db/schema.ts: FOUND (12 pgTable definitions + debt_balances view)
- backend/src/db/client.ts: FOUND
- backend/src/db/migrate.ts: FOUND
- backend/src/db/query-helpers.ts: FOUND
- backend/src/db/migrations/0000_init.sql: FOUND
- backend/src/db/migrations/0001_audit_log_immutability.sql: FOUND (REVOKE executable)
- Commits dc7db1f, 13fb285, ab9c16c: VERIFIED
