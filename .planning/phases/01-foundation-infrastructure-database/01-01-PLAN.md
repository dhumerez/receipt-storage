---
phase: 01-foundation-infrastructure-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - backend/Dockerfile
  - frontend/Dockerfile
  - backend/docker-entrypoint.sh
autonomous: true
requirements:
  - NFR-03.1
  - NFR-03.2
  - NFR-03.3

must_haves:
  truths:
    - "docker compose config runs without errors"
    - "receipts-api binds to 127.0.0.1:4000 only (not 0.0.0.0)"
    - "receipts-frontend binds to 127.0.0.1:4001 only"
    - "receipts-db has NO host port binding"
    - "receipts-db uses a named Docker volume (not bind mount)"
    - "uploads bind-mount path is /var/receipts/uploads on host"
    - "receipts-db healthcheck uses pg_isready"
    - "receipts-api depends_on receipts-db with condition: service_healthy"
    - "docker-entrypoint.sh runs db:migrate before node dist/server.js"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full Docker Compose stack definition"
      contains: "receipts_internal"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "JWT_SECRET"
    - path: "backend/Dockerfile"
      provides: "Multi-stage API container build"
      contains: "healthcheck.js"
    - path: "frontend/Dockerfile"
      provides: "Static frontend container build"
      contains: "node:22-alpine"
    - path: "backend/docker-entrypoint.sh"
      provides: "Container startup script that migrates then starts server"
      contains: "db:migrate"
  key_links:
    - from: "receipts-api"
      to: "receipts-db"
      via: "Docker service name in DATABASE_URL"
      pattern: "receipts-db:5432"
    - from: "docker-compose.yml volumes"
      to: "/var/receipts/uploads"
      via: "bind mount"
      pattern: "/var/receipts/uploads:/var/receipts/uploads"
    - from: "backend/Dockerfile CMD"
      to: "backend/docker-entrypoint.sh"
      via: "ENTRYPOINT [\"sh\", \"/app/docker-entrypoint.sh\"]"
      pattern: "docker-entrypoint.sh"
    - from: "backend/docker-entrypoint.sh"
      to: "npm run db:migrate"
      via: "sequential shell commands before node dist/server.js"
      pattern: "db:migrate"
---

<objective>
Create the Docker Compose stack that runs all three receipts services (API, frontend, DB) coexisting with the Restaurant app already on the Hetzner VPS. Includes a Docker entrypoint script that runs database migrations before starting the API server.

Purpose: Every subsequent phase builds and deploys against this stack. Getting ports, volumes, networking, and migration-on-startup right here prevents painful rewrites during deployment.
Output: docker-compose.yml, .env.example, backend/Dockerfile, frontend/Dockerfile, backend/docker-entrypoint.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure-database/01-RESEARCH.md

Key constraints:
- Restaurant app already uses ports 3000 (API) and 5173 (frontend) on the host
- receipts-api must bind to 127.0.0.1:4000 (NOT 0.0.0.0:4000)
- receipts-frontend must bind to 127.0.0.1:4001
- receipts-db must have NO host port (internal Docker DNS only)
- PostgreSQL named volume (NOT bind mount) — avoids UID 999 permission failure on Linux
- Uploads MUST be a bind mount to /var/receipts/uploads — Nginx on the host reads this path directly
- Migrations MUST run before the API server starts (entrypoint script, not inside server.ts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docker-compose.yml and .env.example</name>
  <read_first>
    .planning/phases/01-foundation-infrastructure-database/01-RESEARCH.md (Pattern 8: Docker Compose Stack)
    .planning/research/ARCHITECTURE.md (Section 2: Docker Compose Configuration)
    .planning/research/PITFALLS.md (Pitfall 13: PostgreSQL Volume Permissions, Pitfall 14: Port Conflict)
    .planning/STATE.md (Notes: existing app uses ports 3000/5173)
  </read_first>
  <files>docker-compose.yml, .env.example</files>
  <action>
Create `docker-compose.yml` at project root with EXACTLY this content (no deviations — every decision is justified in research):

```yaml
services:
  receipts-api:
    build: ./backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:4000:4000"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgres://receipts_user:${DB_PASSWORD}@receipts-db:5432/receipts
      JWT_SECRET: ${JWT_SECRET}
      UPLOAD_DIR: /var/receipts/uploads
      PORT: "4000"
      CORS_ORIGIN: ${CORS_ORIGIN}
    volumes:
      - /var/receipts/uploads:/var/receipts/uploads
    depends_on:
      receipts-db:
        condition: service_healthy
    networks:
      - receipts_internal

  receipts-frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:4001:4001"
    networks:
      - receipts_internal

  receipts-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: receipts
      POSTGRES_USER: receipts_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - receipts_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U receipts_user -d receipts"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - receipts_internal

volumes:
  receipts_db_data:

networks:
  receipts_internal:
    driver: bridge
```

CRITICAL rules encoded above:
- `receipts-db` has NO `ports:` key — zero host exposure
- `receipts_db_data` is a named volume with no driver_opts — Docker manages UID 999 ownership automatically
- `/var/receipts/uploads:/var/receipts/uploads` is a bind mount (not named volume) so Nginx on the host can read files at the same path
- All host port bindings use `127.0.0.1:XXXX:XXXX` pattern (loopback only)

Create `.env.example` at project root:

```
# Database
DB_PASSWORD=change_me_strong_password

# JWT — generate with: openssl rand -hex 64
JWT_SECRET=change_me_generate_with_openssl

# CORS
CORS_ORIGIN=https://receipts.yourdomain.com
```
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config --quiet 2>&1 && echo "VALID" || echo "INVALID"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (no YAML errors)
    - `grep -c "127.0.0.1:400" docker-compose.yml` returns 2 (api and frontend loopback bindings)
    - `grep "ports:" docker-compose.yml | wc -l` returns 2 (only api and frontend have ports)
    - `grep "receipts_db_data:" docker-compose.yml` matches exactly the named volume entry with no `driver_opts`
    - `grep "/var/receipts/uploads:/var/receipts/uploads" docker-compose.yml` finds the bind mount
    - `grep "service_healthy" docker-compose.yml` finds the condition on receipts-api
    - `grep "pg_isready" docker-compose.yml` finds the healthcheck command
  </acceptance_criteria>
  <done>docker-compose.yml passes `docker compose config --quiet`; all port, volume, and network constraints verified by grep</done>
</task>

<task type="auto">
  <name>Task 2: Write Dockerfiles, healthcheck.js, and docker-entrypoint.sh</name>
  <read_first>
    .planning/phases/01-foundation-infrastructure-database/01-RESEARCH.md (Pattern 9: Docker Healthcheck — exact Dockerfile content)
  </read_first>
  <files>backend/Dockerfile, backend/healthcheck.js, backend/docker-entrypoint.sh, frontend/Dockerfile</files>
  <action>
Create directory `backend/` and write `backend/docker-entrypoint.sh`:

```sh
#!/bin/sh
set -e

echo "Running database migrations..."
npm run db:migrate

echo "Starting API server..."
exec node dist/server.js
```

This script is the container ENTRYPOINT. It runs `npm run db:migrate` (which executes `tsx src/db/migrate.ts`) before starting the API server. Using `exec` ensures the node process receives OS signals correctly.

Create `backend/Dockerfile`:

```dockerfile
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:22-alpine AS production
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY healthcheck.js ./
COPY docker-entrypoint.sh ./
RUN chmod +x docker-entrypoint.sh

EXPOSE 4000

HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 \
  CMD node healthcheck.js

ENTRYPOINT ["sh", "/app/docker-entrypoint.sh"]
```

Note: The ENTRYPOINT runs docker-entrypoint.sh, which runs migrations then starts the server. There is NO separate CMD — the entrypoint script calls `exec node dist/server.js` directly.

Create `backend/healthcheck.js` (CommonJS — no build step, no dependencies):

```javascript
const http = require('node:http');
const options = {
  hostname: 'localhost',
  port: parseInt(process.env.PORT ?? '4000', 10),
  path: '/health',
  method: 'GET',
  timeout: 5000,
};
const req = http.request(options, (res) => {
  process.exit(res.statusCode === 200 ? 0 : 1);
});
req.on('error', () => process.exit(1));
req.on('timeout', () => { req.destroy(); process.exit(1); });
req.end();
```

Create directory `frontend/` and write `frontend/Dockerfile`:

```dockerfile
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:22-alpine AS production
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules

EXPOSE 4001

CMD ["node", "dist/server.js"]
```

Note: frontend/Dockerfile uses node dist/server.js because Vite production builds produce a static bundle served by a simple Node HTTP server. Plan 1.5 (frontend project setup) will add the actual server entry. This Dockerfile is correct as a stub.
  </action>
  <verify>
    <automated>test -f backend/Dockerfile && test -f backend/healthcheck.js && test -f backend/docker-entrypoint.sh && test -f frontend/Dockerfile && echo "ALL FILES EXIST" || echo "MISSING FILES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "node:22-alpine" backend/Dockerfile` finds at least two occurrences (multi-stage)
    - `grep "healthcheck.js" backend/Dockerfile` finds the HEALTHCHECK CMD line
    - `grep "EXPOSE 4000" backend/Dockerfile` confirms correct port
    - `grep "EXPOSE 4001" frontend/Dockerfile` confirms correct port
    - `grep "node:http" backend/healthcheck.js` confirms no external deps
    - `grep "process.exit" backend/healthcheck.js` confirms exit code logic
    - `grep "docker-entrypoint.sh" backend/Dockerfile` finds ENTRYPOINT reference
    - `grep "db:migrate" backend/docker-entrypoint.sh` finds the migration command
    - `grep "exec node dist/server.js" backend/docker-entrypoint.sh` finds the server start command
  </acceptance_criteria>
  <done>backend/Dockerfile uses docker-entrypoint.sh as ENTRYPOINT; entrypoint runs db:migrate then exec node dist/server.js; healthcheck.js uses only node:http; all acceptance criteria pass</done>
</task>

</tasks>

<verification>
Full stack verification:
1. `docker compose config --quiet` — exits 0
2. `grep -E "127\.0\.0\.1:400[01]" docker-compose.yml` — finds both port bindings
3. `grep "ports:" docker-compose.yml` — exactly 2 occurrences (db has none)
4. `grep "receipts_db_data:" docker-compose.yml` — named volume exists, no driver_opts
5. `grep "service_healthy" docker-compose.yml` — api waits for db healthcheck
6. `grep "db:migrate" backend/docker-entrypoint.sh` — migration runs before server start
7. `grep "ENTRYPOINT" backend/Dockerfile` — entrypoint script is wired
</verification>

<success_criteria>
- docker-compose.yml is YAML-valid (`docker compose config` exits 0)
- receipts-db has no host port binding
- PostgreSQL uses named volume (not bind mount)
- Upload directory uses bind mount to /var/receipts/uploads
- Both Dockerfiles exist and use node:22-alpine multi-stage builds
- healthcheck.js uses only Node.js built-ins
- backend/docker-entrypoint.sh runs `npm run db:migrate` then `exec node dist/server.js`
- backend/Dockerfile uses docker-entrypoint.sh as ENTRYPOINT (not CMD)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure-database/01-01-SUMMARY.md`
</output>
